import { FC } from 'react'

interface HeroSectionProps {
  
}

const HeroSection: FC<HeroSectionProps> = ({}) => {
  return( 
    <main className='h-screen block bg-[#f7f7f6]'>
        <h1>HERO</h1>
    </main>
)
}

export default HeroSection